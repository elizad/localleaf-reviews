version: '3.8'
services:
  postgres:
    image: postgres:14
    restart: always
    environment:
      - POSTGRES_USER=doadmin
      - POSTGRES_PASSWORD=AVNS_ZwyBkEu5PdYahjdP_Br
    volumes:
      - postgres:/var/lib/postgresql/data
    ports:
      - '5432:5432'
volumes:
  postgres: